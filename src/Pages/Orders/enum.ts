
export enum OrderStatusType {
    'PENDING' = 'PENDING',
    'PROCESSING' = 'PROCESSING',
    'COMPLETE' = 'COMPLETE'
}
export enum OrderStatusEnum {
    'PENDING' = 200,
    'PROCESSING' = 300,
    'COMPLETE' = 400
}